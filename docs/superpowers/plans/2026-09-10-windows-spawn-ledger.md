# SDD ledger — plan: docs/superpowers/plans/2026-09-10-windows-spawn.md

Spec: docs/superpowers/specs/2026-09-10-windows-spawn-design.md (read; binding authority)
Branch: feat/windows-spawn (not master). Base of branch: 0724807.

Ruling: work proceeds on the feature branch in the main checkout rather than a
separate git worktree — why: Tasks 2-7 each end in `git push` + `gh run watch`
because Windows is unverifiable locally, and a worktree adds no isolation while
complicating that flow; nothing else is in progress in this checkout — cost if
wrong: none beyond a dirty main checkout, recoverable with `git switch`.

## Pre-flight conflict scan

Pairs of tasks sharing a file or an interface:

| Tasks | Produces / consumes | Found |
|---|---|---|
| T1 -> T2 | `_ptyproc.py`'s `if sys.platform == "win32"` branch imports `_winpty`, created in T2 | No conflict. The branch carries `# pragma: no cover` on the `if`, so the whole block is excluded and Linux coverage stays at 100% in T1; ruff does not resolve imports; mypy is still pinned to linux in T1 so the branch drops out. |
| T1 -> T4 | both edit `pty_spawn.py` | Disjoint regions: T1 imports/`_spawnpty`/`isatty`/`send`, T4 `_ptyproc_kwargs`/`_ready`/`interact`/`getecho`/`setecho`/`waitnoecho`/`kill`. |
| T1 -> T5 | both edit `pty_spawn.py` | Disjoint: T5 is `terminate` only. |
| T2 -> T5 | T5's Windows `terminate` calls `self.ptyproc.terminate(force=force)` | Provided by T2's adapter. Ordering satisfied. |
| T2 -> T7 | both edit `pyproject.toml` and `noxfile.py` | Disjoint sections: T2 deps/classifier/mypy-winpty-override/ruff-per-file/coverage-exclude_also and `collate_coverage`; T7 `[tool.mypy] platform` and `lint`. |
| T4 -> T5 | T5's POSIX `terminate` calls `self.kill(sig)`, which T4 reroutes through the backend | Compatible: ptyprocess's `kill` is the same `os.kill` call. |
| T5 -> T6 | both edit `tests/conftest.py`; T5's `_time_one_child` wants `commands.CAT` from T6 | Ordering conflict — ruled below. |
| T3 -> T5 | both write `tests/test_spawn_smoke.py` (T3 creates, T5 appends) | No conflict. |
| T4/T2/T3 -> T6 | the three new test modules are not in `_POSIX_ONLY`, so they run on Windows before T6 removes it | Intended; that is how Tasks 2-5 get their Windows verification. |

Each task against its own text:

| Task | Found |
|---|---|
| T1 | **Defect**: the `_REQUIRED` test used `hasattr` on the *class*, but ptyprocess sets `status`, `exitstatus`, `signalstatus`, `flag_eof`, `terminated`, `fd` and `pid` in `__init__` — verified all seven are False on the class. Fixed: the test now checks a live child, and the expected count in step 6 corrected from 22 to 4. |
| T2 | Self-consistent. `winpty.PtyProcess` verified to carry `fileobj`, `pid`, `fd`, `exitstatus`, `isalive`, `wait`, `close(force)`, `write`, `getwinsize`, `setwinsize`; `winpty.WinptyError` is in `_winpty.pyi`. |
| T3 | Self-consistent. |
| T4 | **Defect**: `test_posix_supports_the_same_calls` used `signal.SIGHUP`, which T7's `--platform win32` mypy pass would reject and which does not exist on Windows at all. Fixed: uses the module-level `_SIGHUP = 1` already defined for the Windows half; the now-unused `import signal` removed. |
| T5 | **Defect**: the `killed_pty_children` kill was a conditional expression on `_ON_POSIX`, which mypy does not narrow under `--platform win32`, so `signal.SIGKILL` would error. Fixed: rewritten as an `if sys.platform == "win32":` block, which mypy does narrow. |
| T6 | Self-consistent. |
| T7 | **Defect**: the dual mypy pass ran `--platform win32` over `tests/` as well, where the POSIX-only modules reach for `os.fork`, `signal.SIGHUP` and `signal.SIGKILL` behind skipif marks mypy cannot read. Ruled below. |

Nothing in the plan mandates something the review rubric treats as a defect
(no assertion-free tests, no duplicated logic blocks).

## Pre-flight rulings

Ruling: T5 leaves `_CALIBRATION_COMMAND = "cat"` in place and T6 switches it to
`commands.CAT` — why: `tests/commands.py` does not exist until T6, and T5's
replacement of `_time_one_child` still degrades correctly on Windows, where the
`cat` spawn raises and `_measured_budget` already catches `ExceptionPexpect` and
returns `_BUDGET_FLOOR`, which is exactly today's behaviour — cost if wrong: the
Windows per-test budget is the floor for one task's worth of CI runs.

Ruling: T7's `--platform win32` mypy pass covers `src/` only, while the linux
pass covers `src/` and `tests/` — why: the suite's POSIX-only modules use
`os.fork`, `signal.SIGHUP` and `signal.SIGKILL` at function top level, guarded
by pytest skipif marks that mypy cannot read, so a win32 pass over `tests/`
reports dozens of attr-defined errors about tests that never run there; what the
second pass exists to check is the library's Windows code, all of which is in
`src/` — cost if wrong: a type error in Windows-only *test* code reaches CI
instead of the lint session, where the Windows leg runs those tests anyway.

## Execution

Task 1: dispatched (implementer a4bfceea799cabe95, sonnet), BASE a196ebe

Ruling: no `git push` and no `gh run watch` in any task — why: `gh` is not
installed on this machine, and a push to origin (git@github.com:karrukola/pexpect.git)
is an outward-facing side effect that triggers CI, which the operating norms say
to ask about rather than assume; the user has not asked for a push — cost if
wrong: Tasks 2-6's Windows claims land unverified until someone pushes the
branch, which is one command away and changes no code. Every Linux-provable
gate (pytest, ruff, mypy, the 100% coverage floor) is still run in full.
Task 1: implementer DONE_WITH_CONCERNS, commit 5b7b603 (4/4 new tests, full suite 393 passed, coverage 100%)
Task 1: controller fixed its own docs — `ruff format` formats python fenced in markdown, and the spec and plan broke `nox -s lint`; commit 7c515fc. Verbatim pywinpty quote moved to a `text` fence so reformatting cannot un-quote it.
Task 1: reviewer dispatched (a67f254141b468151, sonnet) over a196ebe..5b7b603
Task 1: complete (commits a196ebe..5b7b603, review clean — spec compliant, quality approved)
Task 1: minor (deferred): `_ptyproc.py:50` `type: ignore[no-redef]` would read oddly to someone who only sees the POSIX branch; wants a one-line comment.
Task 1: minor (deferred): the brief predicted three missed coverage lines from the win32 branch; covdefaults excluded them via the pragma and the run came out at 0 missing. Reviewer could not confirm the exact pragma set from the diff. Harmless — Task 2 replaces the pragma with the exclude_also regexes.
Task 2: dispatched, BASE 7c515fc

Ruling: installed man-db and zsh, restored the coreutils man pages, and replaced
the minimized-Ubuntu /usr/bin/man shim with /usr/bin/man.REAL — why: `nox` is the
local gate for every task and could not pass here, because
tests/integration/test_replwrap.py::test_pager_as_cat reads `man sleep` and the
two zsh tests need zsh; the CI workflow installs exactly these two packages for
the same reason, so this makes the local gate match CI rather than weakening it
— cost if wrong: sandbox state only, nothing in the repo changes.
Verified: tests/integration/test_replwrap.py now 14 passed, was 1 failed 2 skipped.
Task 2: code complete but left uncommitted by the implementer (it parked on a background nox wait three separate times, ~30 min and 142k tokens). Controller ran the gate itself: `uv run --frozen nox` exit 0, all 12 sessions green, collate_coverage enforcing 100% on Linux with _winpty.py omitted. Controller committed as 536ccdc.
Ruling: controller ran the verification gate and made the commit for Task 2 — why: the implementer could not block on a 12-minute command and stalled the loop repeatedly; running a gate and committing already-written code is bookkeeping, not authoring, and the diff still goes through the normal task review — cost if wrong: the commit's author did not personally watch the gate, mitigated by the controller reading the nox verdict directly.
Task 2: reviewer dispatched over 7c515fc..536ccdc, carrying the implementer's four Windows concerns as named risks.
Task 2: review verdict — spec compliant; 1 Critical, 3 Important, 13 Minor. Reviewer read the pywinpty 3.0.5 wheel source directly, so its citations are load-bearing.
Task 2: minor (deferred): docstring said "the child's real bytes" — folded into the Critical fix, since that docstring is being rewritten anyway.
Task 2: minor (deferred): kill() rejects signal.CTRL_C_EVENT (0) and CTRL_BREAK_EVENT (1), the Windows-native values its own docstring names.
Task 2: minor (deferred): a force-killed child is indistinguishable from a clean exit(15) — os.kill(pid, SIGTERM) is TerminateProcess(handle, 15). Windows cannot do better; a flag set by kill() would let a caller tell them apart.
Task 2: minor (deferred): _reap() sets terminated=True unconditionally, so a single None from pty.get_exitstatus() latches exitstatus/status at None forever.
Task 2: minor (deferred): read_bytes does not translate OSError from recv; SpawnBase only converts errno.EIO into EOF.
Task 2: minor (deferred): the exclude_also regex `if sys\.platform != .win32.:` also matches src/pexpect/__init__.py:81, which carries `# pragma: no branch` because the repo wanted it measured. Moot after Task 3, which deletes that guard.
Task 2: minor (deferred): os.fsdecode of a bytes argv is lossy for a non-UTF-8 `encoding=`; unavoidable at this seam, wants a sentence.
Task 2: minor (deferred): the mnemonic dict in sendcontrol is rebuilt per call; noxfile lost the sentence explaining why the floor goes on the command line.
Task 2: minor (deferred): the appended test's spawn() is inert for its own assertions, and spawn(echo=False)/spawn(preexec_fn=)/kill(SIGHUP) have no test — Task 4 covers those.
Task 2: ⚠️ cross-task items the reviewer could not verify, all confirmed as later tasks' scope: pty_spawn.terminate/kill still call os.kill directly (Task 5); ignore_sighup=True installs a preexec_wrapper so the adapter refuses with a message naming preexec_fn, which is misleading (Task 4 must raise for ignore_sighup before that point); use_poll=True reaches select.poll (Task 4); tests/test_unsupported.py does not exist yet (Task 4).
Task 2: fix round 1 dispatched to the original implementer — Critical 1 plus Important 2, 3, 4. Controller added a constraint the reviewer missed: the sentinel arrives on a TCP stream, so an equality check can be defeated by coalescing (`b"0011Ignorehello"`); the fix must handle prefix and embedded occurrences.
Task 2: fix round 1/5 (4 addressed, 0 open; commits 536ccdc..00b1151). Re-review verdict: all findings addressed, no new Critical/Important breakage; sendcontrol/sendeof/sendintr still return (n, byte) so pty_spawn's unpacking is unaffected.
Task 2: minor (deferred): residual sentinel leak for small reads — `read_bytes` strips via bytes.replace, but a `recv(1)` can never contain the whole 10-byte sentinel, so its bytes can still leak one at a time. Reachable via read_nonblocking(size=1), which pxssh.py:275 uses and which is the parameter's default. Strictly better than before (previously 100% of the sentinel leaked at every size) and only on an empty native read, which is rare under the default PYWINPTY_BLOCK=1. FIRST SUSPECT if Windows CI shows corrupted reads in a pxssh-shaped path.
Ruling: the re-review's out-of-scope observation is load-bearing and moves into Task 4's scope — pty_spawn.send() calls self.ptyproc.write_bytes(b) at pty_spawn.py:733 outside _wrap_ptyprocess_err(), so write_bytes's new UTF-8 refusal and its newly-wrapped EOFError surface as PtyProcessError rather than ExceptionPexpect. That breaks the Global Constraint that every unsupported call raises ExceptionPexpect — why: it is the same class of defect Task 4 already fixes for getecho/setecho/kill, and leaving it would ship a documented constraint violated on the commonest call in the library — cost if wrong: one extra context manager in send(), trivially revertible.
Ruling: Task 3 dispatched in parallel with the Task 2 nox gate rather than after it — why: Task 3 touches only __init__.py and a new test file, no overlap with _winpty.py, and the Task 2 fix cannot move Linux coverage because _winpty.py is omitted there; the implementer had already run ruff, mypy and the full suite green — cost if wrong: Task 3 lands on a base whose gate later fails, which the ledger makes recoverable.
Task 3: dispatched, BASE 00b1151
Task 2: complete (commits 7c515fc..00b1151, review clean after 1 fix round). Controller-run gate: `uv run --frozen nox` exit 0, 12/12 sessions green, 100% Linux coverage with _winpty.py omitted.
Ruling: the per-task gate is now ruff + mypy + full pytest (implementer) plus a single-interpreter `coverage run` + `coverage report --omit=*/pexpect/_winpty.py` (controller, ~2 min), instead of the full 12-session `nox` matrix per task — why: nox reads the working tree, so it cannot run concurrently with an implementer, and serialising a 12-minute matrix behind every task stalls the loop; the matrix's extra value over one interpreter is cross-version behaviour, which no task here is version-specific, and the coverage floor — the one gate the fast path could miss — is checked directly. The full matrix runs once before the final whole-branch review — cost if wrong: a version-specific break is caught at the end rather than at the task, and the ledger names the commit range to bisect.
Task 3: implementer DONE, commit bba8642. Gate: 400 passed 1 skipped, 100% coverage (2159 stmts, 580 branches), ruff and mypy clean.
Task 3: reviewer dispatched over 00b1151..bba8642
Task 4: dispatched, BASE bba8642 — scope includes the send() wrapping ruling carried over from Task 2's re-review.
Task 3: complete (commits 00b1151..bba8642, review clean — spec compliant, quality approved, no Critical/Important). Reviewer traced close(force=True) -> exitstatus through pty_spawn.py:450-465 and confirmed the assertion is not vacuous: close() calls isalive() explicitly to update exit status.
Task 3: minor (deferred): tests/test_spawn_smoke.py:66-72 test_fileno_is_a_descriptor asserts only `isinstance(int)` and `>= 0`, which a placeholder value would satisfy. Inherited verbatim from the plan, not an implementer choice. The other three smoke tests carry the detection weight.
Task 4: implementer DONE_WITH_CONCERNS, commit f6b1858. Gate: 401 passed 10 skipped, 100% coverage (2165 stmts, 576 branches), ruff and mypy clean.
Task 4: implementer found a real trap worth recording — the brief's `if use_poll and sys.platform == "win32":` compound condition does not match pyproject's `exclude_also` regex, so it counted as an uncoverable POSIX branch and dropped the floor to 99%. Restructured to the nested form the brief already uses for ignore_sighup. Any future platform guard in this repo must be a bare `if sys.platform ...:` to stay excludable.
Task 4: reviewer dispatched over bba8642..f6b1858
Task 4: complete (commits bba8642..f6b1858, review clean — spec compliant, quality approved, no Critical/Important). Reviewer traced all nine refusal messages to their source and confirmed each names only its own call, and verified all three implementer deviations were genuinely forced.
Task 4: minor (deferred): kill() double-checks isalive() — once in pty_spawn, once inside ptyprocess.kill. Harmless, idempotent.
Task 4: minor (deferred): on POSIX, tests/test_unsupported.py is one real assertion plus nine skips, and that one duplicates coverage test_expect.py and integration/test_waitnoecho.py already give. Its nine Windows tests were traced against _winpty.py's actual guards and are real, not vacuous.
Ruling: the reviewer's ⚠️ moves into Task 5's scope — sendcontrol/sendeof/sendintr at pty_spawn.py:792,808,815 still call the backend unwrapped, so a PtyProcessError from them leaks raw on any platform. On Windows all three route through _winpty.write_bytes, which now raises PtyProcessError for a closed pty, so this is reachable and violates the same Global Constraint that Task 4's send() wrapping was added to satisfy — why: it is one context manager per method, the identical pattern already applied three times in this task, and leaving it means the constraint holds for send() but not for sendintr(), which terminate() itself calls — cost if wrong: three lines, trivially revertible.
Task 5: dispatched, BASE f6b1858 — scope includes wrapping sendcontrol/sendeof/sendintr.
Task 5: implementer DONE_WITH_CONCERNS, commit f0ca5b9 (403 passed 10 skipped, 100% coverage, 2 new tests, no new skips).
Task 5: implementer flagged a real gate failure it did not cause — `ruff check` fails with SIM102 at pty_spawn.py:267, from Task 4's nested `if use_poll: / if sys.platform == "win32":`. Controller verified: "Found 1 error."
PROCESS DEFECT: Task 4 was marked complete with a broken `ruff check`. Its implementer AND its reviewer both reported ruff clean, and the controller's own per-task gate ran only coverage. Ruling: the controller gate now runs ruff format, ruff check, mypy and coverage itself for every remaining task, rather than trusting the implementer's claim — why: two independent agents asserted a green gate that was red, so the claim is not evidence; the check costs seconds — cost if wrong: none.
Ruling: SIM102 is suppressed with `# noqa: SIM102` plus a comment, rather than obeyed — why: ruff's fix (combining the conditions) is exactly what breaks pyproject's `exclude_also` regex, turning the Windows branch into an uncoverable POSIX branch and dropping the floor to 99%; the two rules conflict directly and the coverage floor wins, because a suppressed lint rule is visible in the source while a silently un-excluded branch is not — cost if wrong: one noqa comment in a file that already carries many, all documented.
Task 5: fix dispatched to the live implementer (it holds the file in context and flagged the failure itself).
Task 5: SIM102 fixed in d18c05e with a documented noqa. Implementer also checked the two other nested `if sys.platform` sites (:382-383, :576-577) and confirmed neither trips SIM102 because their outer bodies hold more than the inner if.
Task 5: controller-run gate (all four, by the controller this time): ruff format 107 files clean, ruff check "All checks passed!", mypy clean across 83 files, 403 passed 10 skipped, coverage 100% (2168 stmts, 574 branches).
Task 5: reviewer dispatched over f6b1858..d18c05e
Task 5: complete (commits f6b1858..d18c05e, review clean — spec compliant, quality approved, no Critical/Important). Reviewer verified the SIM102 suppression against the actual exclusion regex and both sibling nested-if sites, checked all line lengths with awk, and confirmed the POSIX terminate ladder is byte-for-byte unchanged.
Task 5: minor (deferred): killed_pty_children's docstring still says "SIGKILL would cut short a child that is writing its own coverage data" while the signal is now _HARD_KILL (SIGTERM on Windows). Accurate for POSIX, which is the only platform where a traced child is realistic.
Task 5: ⚠️ the implementer claimed _winpty.PtyProcess.terminate already catches PtyProcessError from its own sendintr(), making spawn.terminate() never at risk pre-fix. Not verifiable from this diff (_winpty.py is Task 2's file) and not load-bearing either way — the wrapping was still needed for the direct spawn.sendcontrol/sendeof/sendintr paths. Controller note: leave to the final review.
Ruling: Task 6 is split into 6a (tests/commands.py, the four helper programs, and converting the 14 modules' ~127 command literals) and 6b (deleting _POSIX_ONLY/collect_ignore, adding per-module skipif to the five remaining POSIX-only modules, the tests/integration directory-wide skip, and switching _CALIBRATION_COMMAND to commands.CAT) — why: they are independently reviewable and a reviewer could legitimately accept one and reject the other; a single ~19-file diff mixing a mechanical literal conversion with a collection-mechanism change is one no reviewer can judge properly, and the plan's own Task Right-Sizing rule says to split where that is true — cost if wrong: one extra review seat.
Task 6a: dispatched, BASE d18c05e
Task 6a: implementer DONE, commit 2a26465. Lint gate clean (ruff check, ruff format 113 files, mypy 89 files). Test counts identical to baseline: 403 passed, 10 skipped.
CONTROLLER ERROR: the Task 6a dispatch named .superpowers/sdd/.../task-6-brief.md, which had never been generated — task-brief was run for tasks 1-5 only. The implementer noticed, fell back to the plan doc's Task 6 section, and said so. Brief generated afterwards for the reviewer. No work was done from a guess.
Task 6a: implementer deviated from the brief once, correctly — the brief said `" ls"` needs no skip because it is about split_command_line, but test_leading_whitespace_resolves_same_command actually spawns and awaits real `ls`, so it got a skipif.
Task 6a: two items carried to 6b — test_run.py's four `bash --norc` tests and one `Popen(["uname", ...])` comparison have neither a stand-in nor a skip, and fall outside the brief's four named categories. They would fail on Windows.
Task 6a: coverage gate green — 403 passed 10 skipped, 100% (2168 stmts, 574 branches), identical to the pre-conversion baseline.
Task 6a: review verdict — Needs fixes. Substitution itself verified line by line as genuinely mechanical (no assertion, pattern, docstring or timeout altered anywhere), and the implementer's one deviation from the plan was correct. Two gaps found, BOTH PLAN DEFECTS OF MINE:
  - tests/test_log.py drives cat (:54,:69,:89) and echo (:42) and sits in _POSIX_ONLY, but appears in neither Task 6's convert list nor its five-module skip list. After 6b it would collect on Windows with four unconverted POSIX literals and no skip.
  - tests/test_misc.py:530-536 test_cwd drives `pwd`, a fifth POSIX-only program the plan never measured, in a module 6a did convert.
Task 6a: reviewer CORRECTED two claims in the implementer's own report — the two-argument spawn(cmd, [args]) path is NOT uncovered (tests/test_spawn_smoke.py:31,63,74, tests/utils.py:29, tests/test_isalive.py:67 all use it, and that module is not in _POSIX_ONLY), and the Popen(["uname"]) call at test_run.py:99 IS handled by the skipif at :93. The genuinely unhandled test_run.py items are the four `bash --norc` calls at :148,:157,:166,:177 — real 6b work.
Ruling: three Minor findings folded into the fix round instead of deferred — quoting the paths in commands.py (`utils.py:84-89` checks quote state before the backslash branch, so quoting survives both spaces and native separators, where as_posix alone survives only separators); substituting `" " + commands.TRUE` at test_constructor.py:66 instead of skipping, keeping a test about pexpect's own argument parsing alive on Windows; and adding tests/test_commands.py so the four stand-ins are executed by a gate rather than by a reviewer's hand check — why: each is a few lines and each removes real Windows-leg risk, which is the one thing this environment cannot verify later — cost if wrong: a slightly larger fix diff.
Task 6a: minor (deferred): test_expect.py:390 test_expect_exact is half an `ls -l /bin` diff and half a portable echo/expect_exact check; the whole-test skip means literal-dot expect_exact gets no Windows coverage. Splitting it would keep that half running.
Task 6a: minor (deferred): commands.echo(text) appends its argument raw, so it is really echo(fragment) — test_dotall.py:44,50 pass pre-quoted values. Docstring says "prints *text*".
Task 6a: WINDOWS RISK, recorded for the CI leg: cat.py writes via sys.stdout.write(line), and Windows text-mode \n -> \r\n translation on top of ConPTY could yield \r\r\n where test_misc.py::test_read asserts child.read(2) == b"\r\n" and test_iter asserts b"abc\r\n123\r\n". If seen, the fix belongs in the stand-in (sys.stdin.reconfigure(newline="") or writing through sys.stdout.buffer), NEVER in the assertion.
Task 6a: fix round 1 dispatched — Important 1 and 2 plus the three folded minors.
Task 6a: fix round 1/5 (5 addressed, 0 open; commits 2a26465..401eed0). Re-review: all addressed, no new breakage. Verified the quoting against utils.py:70-108 — the quote-state branch precedes the backslash branch, so quoting closes the space and backslash cases at once. Test-count accounting confirmed: 403 -> 407, all four from tests/test_commands.py, every other change a win32-only skipif that is inert on Linux.
Task 6a: complete (commits d18c05e..401eed0, review clean after 1 fix round). Controller gate: ruff check clean, ruff format 114 files, mypy clean 90 files, 407 passed 10 skipped, coverage 100% (2168 stmts, 574 branches).
Ruling: the re-review's carried note moves into 6b's scope — tests/test_commands.py exercises native cat/true/echo/sleep on Linux, because commands.py dispatches on platform, so tests/helpers/*.py still get their first execution anywhere on the Windows CI leg. 6b adds a platform-independent test that drives the four stand-ins through commands._helper() directly — why: the stand-ins are the deliverable the whole Windows suite rests on, this environment cannot reach Windows CI to find out they work, and a syntax error or broken loop in one of them is catchable here for about five lines; folding it into 6b rather than opening a sixth fix round keeps the loop honest — cost if wrong: five lines of test in the wrong module.
Task 6b: dispatched, BASE 401eed0
Task 6b: implementer DONE, commit e60b546. Controller gate: ruff check clean, ruff format 114 files, mypy clean 90 files, 411 passed 10 skipped, coverage 100% (2168 stmts, 574 branches).
Task 6b: review verdict — Needs fixes. Conversion itself verified correct against every named risk: all five pytestmark edits preserved their existing fixtures (test_pxssh kept all three), the integration skip was merged into the EXISTING pytest_collection_modifyitems rather than defining a second, the 407->411 accounting is exact, and the new stand-in tests call commands._helper() unconditionally with assertions tight enough to catch content corruption.
Task 6b: PLAN DEFECT OF MINE — tests/test_replwrap.py's skip reason "drives bash and zsh" is false. Controller read the module: its docstring says it starts no shell and that everything driving a real shell lives in tests/integration/test_replwrap.py; its single test mocks PATH and asserts ExceptionPexpect for a missing binary, structurally identical to test_missing_command.py, which needs no skip. The phrasing was inherited from the old _POSIX_ONLY comment, written before that module was split from its integration half. Ruling: drop the skip entirely — why: the file no longer does what the reason claims, and a skip with a false reason is worse than no skip because it tells the next reader the module cannot run on Windows when it can — cost if wrong: one Windows-only test failure, visible on the first CI run.
Task 6b: second Important — the new tests/conftest.py:144-148 calibration comment says commands.CAT "either way starts without ... an interpreter of its own", but on Windows it starts a Python interpreter. That comment is the documented justification for what the per-test budget measures, so a future reader tuning the budget would reason from a false premise.
Task 6b: ⚠️ reviewer noted --collect-only ran on Linux and therefore establishes importability on Linux, not on Windows, which is the precondition the skipif mechanism actually needs. It spot-checked pexpect.replwrap, socket_pexpect and popen_spawn for unconditional POSIX-only top-level imports and found none, so Windows import success is plausible, not proven. The Windows CI leg is the real check.
Task 6b: minor (deferred): tests/test_commands.py now carries two parallel tracks of the same four assertions — the pre-existing four via commands.CAT/TRUE/echo/sleep, and the new four via commands._helper(). That is what the addition asked for.
Task 6b: fix round 1 dispatched — both Important findings.
Task 6b: fix round 1/5 (2 addressed, 0 open; commits e60b546..46efba7). Controller verified both directly: zero skipif occurrences left in tests/test_replwrap.py, and the conftest calibration comment now states the Windows interpreter cost and why a looser budget still catches a hang. ruff and mypy clean, 411 passed 10 skipped unchanged.
Ruling: _WINDOWS_COVERAGE_FLOOR stays at 46 rather than being retuned — why: Task 7 Step 5 says to set it from the figure the Windows CI run prints, and this environment cannot reach CI, so any new number would be a guess; guessing high breaks the Windows leg, and the value is a floor, so leaving it low cannot break anything, it only fails to catch a regression until someone reads the first real Windows run. A comment must say so at the constant — cost if wrong: the Windows coverage gate is loose for one CI cycle, and the ledger and the comment both name it as the follow-up.
Task 6b: re-review dispatched over e60b546..46efba7 (cheap tier — two changes, both already verified by the controller).
Task 7: dispatched, BASE 46efba7
Task 6b: complete (commits 401eed0..46efba7, review clean after 1 fix round). Re-review: both findings addressed, no new breakage; the now-dead `import sys` was cleaned up with the skip, and every clause of the rewritten calibration comment was checked against what commands.CAT resolves to on each platform.
Task 7: implementer DONE_WITH_CONCERNS, commit 8a0f417. Controller gate: ruff check clean, mypy --platform linux clean (90 files), mypy --platform win32 clean (19 files), 411 passed 10 skipped.
Task 7: the win32 mypy pass surfaced three errors the plan did not predict, none inside an existing sys.platform branch: select.poll/POLLIN in utils.py, _spawnpty's **kwargs: object in pty_spawn.py, and signal.SIGKILL in popen_spawn.py's close() escalation. Each got a specifically-coded type: ignore with a comment.
Ruling: the popen_spawn.py SIGKILL ignore is being replaced by a real fix, widening this task's stated no-behaviour-change scope on purpose — why: PopenSpawn is the class pexpect documents FOR Windows, and PopenSpawn.close() reaching signal.SIGKILL raises AttributeError there while evaluating the argument (kill() itself already maps non-console signals to SIGTERM on Windows, so the failure is the argument, not the call); tests/test_popen_spawn.py skips on Windows so nothing else catches it; and the project's goal is that pexpect's methods work on Windows, which this one does not. subprocess.Popen.kill() is SIGKILL on POSIX and TerminateProcess on Windows, so the fix is behaviour-identical on POSIX — cost if wrong: one line in a method whose POSIX tests do exercise it.
Task 7: the other two ignores are provisionally accepted as honest stub gaps rather than masked defects — utils.py's select.poll is unreachable on Windows because use_poll=True already raises ExceptionPexpect, and _spawnpty's **kwargs: object is a typing limitation. Implementer asked to confirm or contradict both.
Task 7: SIGKILL bug fixed in 786d443 — self.proc.kill() replaces self.kill(signal.SIGKILL), behaviour-identical on POSIX (send_signal(SIGKILL) either way) and TerminateProcess on Windows. test_popen_spawn.py's test_close_kills_a_child_that_ignores_sigterm exercises that exact rung and passes. Implementer confirmed the other two type: ignores are genuine stub gaps, not masked defects.
CONTROLLER ERROR, corrected: a coverage run reported 99% and the controller announced a regression. False alarm — that run had been started in the background and then overlapped with the implementer editing popen_spawn.py underneath it, so it measured a file mid-write. Re-run on a quiet tree: 100% (2169 stmts, 574 branches, 0 missing), 411 passed 10 skipped. Lesson: never leave a background gate running while an implementer is live in the same tree.
Task 7: reviewer dispatched over 46efba7..786d443
Task 7: complete (commits 46efba7..786d443, review clean — spec compliant, quality approved, no Critical/Important). Reviewer independently traced every Windows claim in all five doc files to _winpty.py, pty_spawn.py and tests/test_unsupported.py, and verified the skip inventory against source: four module-level skipifs plus the tests/integration directory skip, and test_replwrap.py carries none. NOTE: the controller's own review prompt said "six modules"; the implementer checked source instead of copying it, and the docs say "four modules and one whole directory", which is correct.
Task 7: minor (deferred): the review-46efba7..786d443.diff package truncated mid-hunk in its last file; the reviewer read the live file to confirm nothing material was hidden. Affects the diff tooling, not the code.
ALL SEVEN TASKS COMPLETE. Proceeding to the full nox matrix and the final whole-branch review.
FULL NOX MATRIX on the finished branch: exit 0, 12/12 sessions green — lint on five interpreters with both mypy platform passes, tests on five interpreters, collate_coverage enforcing the 100% Linux floor with _winpty.py omitted.
Final whole-branch review dispatched over 0724807..HEAD (19 commits).

## Final whole-branch review

Verdict: Needs fixes before merge. 1 Critical, 5 Important, ~10 Minor. Reviewer read the pywinpty 3.0.5 wheel source directly rather than the spec's summary of it, so its _winpty.py citations are load-bearing.
Ruling OVERTURNED by the final review, and it was right: the residual sentinel leak at small read sizes is Critical, not a deferred minor. My deferral rested on "only on an empty native read, which is rare under the default PYWINPTY_BLOCK=1"; winpty/ptyprocess.py:353 shows an empty native read IS the blocking-mode EOF signal, so it is the common case. Ten spurious bytes reach the caller before EOF at any size below 10 — which is read_nonblocking's default and what pxssh.py:275 uses in a loop. Cost of my error if it had shipped: corrupted reads on the commonest Windows path, invisible to every gate in this environment. The fix is ~6 lines and was provable on Linux all along, which is the part that should not have been deferred.
Final review also found: ~35 tests across six of the fourteen converted modules will fail on the first Windows run from known refusals (echo=False, setecho, waitnoecho, use_poll, ignore_sighup, SIGALRM, and two modules whose child helpers import termios/fcntl) — burying the four real unknowns that run was supposed to test; four backend calls that can raise WinptyError or OSError outside pexpect's wrapper, contradicting _winpty.PtyProcessError's own docstring; DEVELOPERS.rst asserting a Windows run that never happened; two undocumented Windows behaviour differences on supported paths (send() refusing non-UTF-8, read() not byte-exact in bytes mode); and pexpect.replwrap being wholly unusable on Windows while the spec implies it works.
Rulings the final review AGREED with: leaving _WINDOWS_COVERAGE_FLOOR at 46 (a floor can only fail closed, and the alternative was a guess), leaving tests/helpers/cat.py's newline handling alone (guessing at Windows translation layered on ConPTY yields two bugs instead of one), and both surviving type: ignores (verified unreachable / a real typing limitation).
Final review's sharpest structural point: pyproject.toml:196 sets ignore_missing_imports for winpty, so on Linux the entire pywinpty surface is Any and the --platform win32 pass does NOT type-check _winpty.py's use of it. "Type-checks both platforms" is true of pty_spawn.py and false of the one file that most needs it. Only nox -s lint on a Windows runner checks it for real.
Single fix wave dispatched (opus) over the full findings list, with instructions to test Critical 1 on Linux against a scripted fake socket.
Final fix wave: 5 commits 2fcb064..cd5f1b8, split by kind (sentinel buffer, test marks, exception contract, two docs commits). Controller gate: ruff format 115 files, ruff check clean, mypy --platform linux clean (91 files), mypy --platform win32 clean (19 files), 423 passed 12 skipped, coverage 100% (2175 stmts, 578 branches, 0 missing, 0 partial).
Final fix wave: the implementer found that the required fix would have been a WORSE bug on its own — bytes buffered inside the adapter are invisible to select(), so read_nonblocking(size=1) would time out holding 1023 bytes and break every pxssh login. It added pending() to the backend surface (_winpty.py:175, _ptyproc.py:50) and consults it in _ready() (pty_spawn.py:587). That is contract the findings did not ask for and is not optional; the scoped re-review is scrutinising it hardest.
Final fix wave: three other declared deviations — terminate()'s Windows arm moved INSIDE POSIX's try/except rather than merely wrapped (wrapping changes the exception type but leaves the behaviour difference); the short-write check compares a character count rather than len(data), because pywinpty documents no unit for PTY.write()'s int and an equality check could raise on every non-ASCII send() on the first Windows run; and repeated skip reasons are named mark objects rather than fifteen copies of a decorator.
Scoped re-review dispatched over 786d443..cd5f1b8 (opus).
Scoped re-review of the final fix wave: all six findings ADDRESSED, all four declared deviations judged SOUND (including pending(), whose premise the reviewer re-verified in the wheel — winpty/ptyprocess.py:43-56 never calls setblocking/settimeout, so fileobj is a plain blocking socket and buffered bytes really are invisible to select).
NEW HIGH introduced by the fix wave: _winpty.py:214-235 read_bytes() can re-enter a blocking recv() with no data while the child is alive, hanging forever. When the whole undelivered buffer is a proper prefix of the sentinel, _pending_count() is 0 and _peer_closed is false, so the loop recv()s again. Reviewer demonstrated it by executing the module against a stand-in whose recv reports would-block. Reachable on an ordinary path: ConPTY echo is always on because echo=False is refused, so a caller's send("0") echoes back as its own one-byte chunk and the child then waits. The predecessor code reached its retry only for a chunk that was EXACTLY the sentinel, which under PYWINPTY_BLOCK=1 happens just before the FIN, so that recv returned promptly — the widening is new.
Ruling: a second fix wave is being dispatched, overriding the process rule that there is exactly one — why: this is a new infinite hang on an ordinary path, introduced by the wave itself, which defeats the timeout contract that is the whole reason pexpect exists; the fix is ~3 lines, the reviewer named it, and the Linux harness to prove it already exists from this wave. Shipping it as a "recorded limitation" would be following the wave-count rule against its own purpose, which is to stop spiralling, not to ship hangs — cost if wrong: one more review cycle on a narrow diff.
Second wave also carries: marks for tests/test_commands.py:29,58 (two more known-refusal failures in the stand-ins' own module); the withheld-tail latency written into doc/overview.rst, which is the condition the reviewer set for shipping it; pyproject.toml:120-127 still enumerating exceptions that are not exhaustive (pty_spawn.py:884 and popen_spawn.py:136,142,297); the short-write message mixing units; and an over-claim in _winpty.py:206-210 and the spec that the sentinel lands "every time a child exits", which the wheel does not establish.
Second fix wave: commits 30a3214 (hang fix) and 5828cfa (four corrections plus two more Windows skips). Controller gate: ruff and both mypy passes clean, 426 passed 12 skipped, coverage 100% (2175 stmts, 578 branches, 0 missing).
Narrow re-review of the hang fix: ADDRESSED. Traced eight input cases with no surviving hang or byte-loss path; confirmed the new tests fail against cd5f1b8 and pass after, on a stand-in that keeps a real os.pipe() behind fileno() so the select() under test is real. All five corrections accurate. The implementer's dispute about popen_spawn.py:142 was checked programmatically against the exclude_also regex and is RIGHT — "if sys.platform" is not a contiguous substring of that compound line, so it never matched and is covered on Linux; :136 and :297 are excluded by covdefaults' `# pragma: win32 cover`, a different mechanism. Verdict: ready to merge, two Low items.
Ruling: both Low items are being fixed rather than parked — pending() gained real I/O in the hang fix but _ready() calls it unwrapped, so a select() failure would surface as raw PtyProcessError, which is precisely the contract hole the first wave's Important 4 closed everywhere else; and doc/overview.rst's "Two things behave differently" lead-in now undercounts its own section. Why: each is one line, and shipping a self-inflicted contract hole as a recorded follow-up when the fix is a single wrap is not a trade worth making — cost if wrong: one more narrow verification pass.
Final commit 62a242f. FULL NOX MATRIX: exit 0, 12/12 sessions. 426 passed 12 skipped, coverage 100% (2176 stmts, 578 branches). 27 commits from 0724807. Tree clean. BRANCH COMPLETE, unpushed per the standing ruling.
